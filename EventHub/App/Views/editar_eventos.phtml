<?php
  require_once '../App/Auth.php';
  requireLogin();
?>

<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Editar Evento — EventHub</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  
  <style>
    body {
      background-color: #f8f9fa;
    }

    .form-section {
      max-width: 700px;
      margin: 50px auto;
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .form-section h2 {
      text-align: center;
      margin-bottom: 25px;
    }

    .preview-img {
      display: block;
      margin: 10px auto;
      border-radius: 10px;
      max-width: 100%;
      height: auto;
    }
  </style>
</head>

<body>

  <!-- Navbar EventHub -->

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="#">🎉 EventHub</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/home">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos">Painel de Eventos</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos/sobre">Sobre</a></li>
          <?php if (isset($_SESSION['tipo']) && $_SESSION['tipo'] == 'ADMIN'): ?>
            <li class="nav-item"><a class="nav-link" href="/admin/painel">Painel Admin</a></li>
          <?php endif; ?>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                Perfil
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <!-- Editar -->
                    
                    <form id="formEditarPerfil" action="/usuario/editar" method="POST" style="display:none;">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="document.getElementById('formEditarPerfil').submit();">
                            Editar Perfil
                        </a>
                    </li>

                    <!-- Deletar -->

                    <form id="formExcluirPerfil" action="/usuario/excluir" method="POST" style="display:none;" onclick="return confirm('Tem certeza que deseja excluir este usuário? você também vai excluir os eventos dele.')">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="if (confirm('Tem certeza que deseja excluir sua conta? Isso também excluirá seus eventos.')) { document.getElementById('formExcluirPerfil').submit(); } return false;">
                            Excluir Usuario
                        </a>
                    </li>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item text-danger" href="/login/logout">Sair</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Formulário -->
  <section class="form-section">
    <h2>Editar Evento</h2>
      <form action="/eventos/editar" method="POST" enctype="multipart/form-data">
        <input type="hidden" name="id_evento" value="<?= $this->view->evento['id_evento'] ?>">

        <div class="mb-3">
            <label for="nome" class="form-label">Título do Evento</label>
            <input type="text" name="nome" class="form-control" value="<?= $this->view->evento['nome'] ?>" required>
        </div>

        <div class="mb-3">
            <label for="data" class="form-label">Data</label>
            <input type="datetime-local" name="data" class="form-control" value="<?= date('Y-m-d\TH:i', strtotime($this->view->evento['data'])) ?>">
        </div>

        <div class="mb-3">
            <label for="local" class="form-label">Local</label>
            <input type="text" name="local" class="form-control" value="<?= $this->view->evento['local'] ?>" required>
        </div>

        <div class="mb-3">
            <label for="detalhes" class="form-label">Detalhes</label>
            <textarea name="detalhes" class="form-control" rows="3" required><?= $this->view->evento['detalhes'] ?></textarea>
        </div>

        <div class="mb-3">
            <label for="imagem" class="form-label">Imagem do Evento</label>
            <input type="file" name="imagem" class="form-control" accept="image/*">
            <?php if($this->view->evento['imagem']): ?>
                <img src="/<?= $this->view->evento['imagem'] ?>" alt="Imagem do Evento" class="preview-img mt-2">
            <?php endif; ?>
        </div>

        <div class="text-center">
            <button type="submit" class="btn btn-success btn-lg">Salvar Evento</button>
        </div>
    </form>

  </section>

  <!-- Footer -->
  <footer class="fixed-bottom bg-dark text-white text-center py-3 mt-5">
    © Victor Emanuel da Silva Moraes
  </footer>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  
</body>
</html>
