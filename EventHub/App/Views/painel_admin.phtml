<?php
  require_once '../App/Auth.php';
  requireLogin();
  requireTipo();

?>

<!doctype html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Painel Admin — EventHub</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

  <style>
 
  .carousel-item img {
    height: 400px;
    object-fit: cover; 
    border-radius: 10px; 
  }

  #carouselEventos {
    max-width: 1000px; 
    margin: 0 auto; 
  }

 section.py-5.text-white {
    padding-top: 2rem !important;
    padding-bottom: 2rem !important;
  }

  .card-img-top,
  .card img {
    width: 100%;           
    height: 180px;        
    object-fit: cover;    
    border-top-left-radius: .5rem; 
    border-top-right-radius: .5rem;
    display: block;
  }

  .card.h-100 {
    display: flex;
    flex-direction: column;
  }

  .card.h-100 .card-body {
    flex: 1 1 auto; 
  }

</style>


<body>

  <!-- Navbar EventHub -->

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="#">🎉 EventHub</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/home">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos">Painel de Eventos</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos/sobre">Sobre</a></li>
          <?php if (isset($_SESSION['tipo']) && $_SESSION['tipo'] == 'ADMIN'): ?>
            <li class="nav-item"><a class="nav-link active" href="#">Painel Admin</a></li>
          <?php endif; ?>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                Perfil
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <!-- Editar -->
                    
                    <form id="formEditarPerfil" action="/usuario/editar" method="POST" style="display:none;">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="document.getElementById('formEditarPerfil').submit();">
                            Editar Perfil
                        </a>
                    </li>

                    <!-- Deletar -->

                    <form id="formExcluirPerfil" action="/usuario/excluir" method="POST" style="display:none;" onclick="return confirm('Tem certeza que deseja excluir este usuário? você também vai excluir os eventos dele.')">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="if (confirm('Tem certeza que deseja excluir sua conta? Isso também excluirá seus eventos.')) { document.getElementById('formExcluirPerfil').submit(); } return false;">
                            Excluir Usuario
                        </a>
                    </li>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item text-danger" href="/login/logout">Sair</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <section class="py-5 text-center bg-light">
    <div class="container">
      <h1 class="display-4">Painel do Administrador</h1>
      <p class="lead">Painel completo para controle total da plataforma: organize, supervisione e mantenha tudo funcionando perfeitamente.</p>
    </div>
  </section>

  <section class="py-5">
    <form action="/eventos/detalhes/" method="POST">
      <div class="container">
        <div class="row">
            <div class="col-md-12">
              <h2 class="mb-4">Todos os Eventos</h2>
                <div class="row row-cols-1 row-cols-md-3 g-4">

                <?php foreach($this->view->eventos as $evento): ?>

                  <div class="col">
                    <div class="card h-100">
                      <img src="/<?= $evento['imagem'] ?>" class="card-img-top" alt="Imagem">
                      <div class="card-body">
                        <h5 class="card-title"><?= $evento['nome'] ?></h5>
                        <p class="card-text">Data: <?= $evento['data'] ?></p>
                        <p class="card-text">Local: <?= $evento['local'] ?></p>
                        <div>
                          <form action="/eventos/detalhes/" method="POST" class="d-inline-block">
                            <input type="hidden" name="id_evento" value="<?= $evento['id_evento'] ?>">
                            <button type="submit" class="btn btn-success">Detalhes</button>
                          </form>
                          <form action="/eventos/editar" method="POST" class="d-inline-block">
                            <input type="hidden" name="id_evento" value="<?= $evento['id_evento'] ?>">
                            <button type="submit" class="btn btn-primary">Editar</button>
                          </form>
                          <form action="/eventos/deletar" method="POST" class="d-inline-block" onclick="return confirm('Tem certeza que deseja excluir este evento?')">
                            <input type="hidden" name="id_evento" value="<?= $evento['id_evento'] ?>">
                            <button type="submit" class="btn btn-danger">Deletar</button>
                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                <?php endforeach; ?>
              </div>
            </div>

            <div class="col-md-12 mt-5">
                <h2 class="mb-4">Todos os Usuarios</h2>
                  <div class="g-4">
                    <div class="col table-responsive">
                      <table class="table table-striped-columns"> 
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>NOME</th>
                                <th>EMAIL</th>
                                <th>TELEFONE</th>
                                <th>AÇÕES</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach($this->view->usuario as $usuario): ?>
                              <tr>
                                  <td><?=$usuario['id_usuario']?></td>
                                  <td><?=$usuario['nome']?></td>
                                  <td><?=$usuario['email']?></td>
                                  <td><?=$usuario['telefone']?></td>
                                  <td>
                                     <form action="/usuario/editar" method="POST" class="d-inline-block">
                                      <input type="hidden" name="id_usuario" value="<?= $usuario['id_usuario'] ?>">
                                      <button type="submit" class="btn btn-primary">Editar</button>
                                    </form>
                                    <form action="/usuario/excluir" method="POST" class="d-inline-block" onclick="return confirm('Tem certeza que deseja excluir este usuário? você também vai excluir os eventos dele.')">
                                      <input type="hidden" name="id_usuario" value="<?= $usuario['id_usuario'] ?>">
                                      <button type="submit" class="btn btn-danger">Deletar</button>
                                    </form>
                                  </td>
                              </tr>
                            <?php endforeach; ?>
                        </tbody>
                      </table>
                    </div>
                  
                </div>
              </div>
            </div>
        </div>
        
    </form>
  </section>

  <footer class="fixed-bottom bg-dark text-white text-center py-3">
    © Victor Emanuel da Silva Moraes
  </footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>