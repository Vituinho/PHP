<?php
  require_once '../App/Auth.php';
  requireLogin();
?>

<!doctype html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Detalhes — EventHub</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
    /* Mantive estilos mínimos — Bootstrap faz a maior parte do trabalho */
    .hero {

      color: black;
      padding: 3.5rem 1rem;
      border-bottom-left-radius: 0.6rem;
      border-bottom-right-radius: 0.6rem;
    }
    .event-meta .badge { font-size: 0.95rem; }
    .img-hero {
      object-fit: cover;
      width: 100%;
      height: 100%;
      max-height: 520px;
    }
    @media (max-width: 767px) {
      .hero { padding: 2rem 1rem; }
    }
  </style>

</head>

<body>

    <!-- Navbar EventHub -->

  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <a class="navbar-brand" href="#">🎉 EventHub</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/home">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos">Painel de Eventos</a></li>
          <li class="nav-item"><a class="nav-link" href="/eventos/sobre">Sobre</a></li>
          <?php if (isset($_SESSION['tipo']) && $_SESSION['tipo'] == 'ADMIN'): ?>
            <li class="nav-item"><a class="nav-link" href="/admin/painel">Painel Admin</a></li>
          <?php endif; ?>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                Perfil
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li>
                    <!-- Editar -->
                    
                    <form id="formEditarPerfil" action="/usuario/editar" method="POST" style="display:none;">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="document.getElementById('formEditarPerfil').submit();">
                            Editar Perfil
                        </a>
                    </li>

                    <!-- Deletar -->

                    <form id="formExcluirPerfil" action="/usuario/excluir" method="POST" style="display:none;" onclick="return confirm('Tem certeza que deseja excluir este usuário? você também vai excluir os eventos dele.')">
                        <input type="hidden" name="id_usuario" value="<?= $_SESSION['id_usuario'] ?>">
                    </form>

                    <li>
                        <a class="dropdown-item" href="#" onclick="if (confirm('Tem certeza que deseja excluir sua conta? Isso também excluirá seus eventos.')) { document.getElementById('formExcluirPerfil').submit(); } return false;">
                            Excluir Usuario
                        </a>
                    </li>
                </li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item text-danger" href="/login/logout">Sair</a></li>
            </ul>
          </li>
        </ul>
      </div>
    </div>
  </nav>


        <?php if(isset($this->view->evento) && $this->view->evento): ?>

        <!-- HERO com imagem e título grande -->
        <header class="hero">
          <div class="container">
            <div class="row g-3 align-items-center">
              <div class="col-lg-8">
                <h1 class="display-6 fw-bold mb"><?= $this->view->evento['nome'] ?></h1>
                <div class="event-meta d-flex flex-wrap gap-2 align-items-center">
                  <span class="badge bg-warning text-dark">Data: <?= $this->view->evento['data'] ?></span>
                  <span class="badge bg-success">Local: <?= $this->view->evento['local'] ?></span>
                </div>
              </div>

              <div class="col-lg-4 text-lg-end">
                <!-- Call-to-action visual (sem JS) -->
                <div class="mt-3 mt-lg-0">
                  <p class="mb-1 text-black-50">Organizador: <?= $this->view->evento['nome_usuario'] ?></p>
                  <h6 class="text-black fw-semibold"><?= $this->view->evento['nome'] ?></h6>
                  <p class="small text-black-50 mb-2">Contato: <?= $this->view->evento['telefone_usuario'] ?> • <?= $this->view->evento['email_usuario'] ?></p>
                </div>
              </div>
            </div>
          </div>
        </header>

        <!-- Conteúdo principal -->
        <main class="container my-3">
          <div class="row g-4">
            <!-- Coluna imagem grande -->
            <div class="col-lg-7">
              <figure class="card shadow-sm">
                <!-- Imagem em alta qualidade (troque a URL se quiser outra imagem) -->
                <img
                  src="/<?= $this->view->evento['imagem'] ?>"
                  sizes="(max-width: 768px) 100vw, 700px"
                  alt="Imagem ilustrativa do evento"
                  class="img-hero card-img-top"
                  loading="lazy"
                />
                <figcaption class="card-body">
                  <div class="d-flex justify-content-between align-items-start mb-3">
                    <div>
                      <h5 class="card-title mb-1"><?= $this->view->evento['nome'] ?></h5>
                    </div>
                    <div class="text-end">
                      <small class="text-muted">Alta resolução</small>
                    </div>
                  </div>

                  <p class="card-text">
                    <?= $this->view->evento['detalhes'] ?>
                  </p>
                </figcaption>
              </figure>
            </div>

            <!-- Coluna com informações detalhadas e extras -->
            <aside class="col-lg-5">
              <div class="card shadow-sm mb-4">
                <div class="card-body">
                  <h5 class="mb-3">Detalhes completos</h5>

                  <dl class="row">
                    <dt class="col-4 text-muted">Nome do evento</dt>
                    <dd class="col-6"><?= $this->view->evento['nome'] ?></dd>

                    <dt class="col-4 text-muted">Data</dt>
                    <dd class="col-6"><?= $this->view->evento['data'] ?></dd>

                    <dt class="col-4 text-muted">Local</dt>
                    <dd class="col-6"><?= $this->view->evento['local'] ?></dd>

                    <dt class="col-4 text-muted">Organizador</dt>
                    <dd class="col-6"><?= $this->view->evento['nome_usuario'] ?></dd>
                  </dl>

                  <hr />

                  <h6 class="mb-2">Contato e links úteis</h6>
                  <p class="small text-muted mb-1">E-mail</p>
                  <p class="mb-2"><?= $this->view->evento['email_usuario'] ?></p>

                  <p class="small text-muted mb-1">Telefone</p>
                  <p class="mb-3"><?= $this->view->evento['telefone_usuario'] ?></p>
                  <?php
                  $nomeUsuario = $this->view->evento['nome_usuario'];
                  $telefone = $this->view->evento['telefone_usuario'];

                  $telefone_numeros = preg_replace('/\D/', '', $telefone);

                  if(strlen($telefone_numeros) === 10 || strlen($telefone_numeros) === 11) {
                      $telefone_numeros = '55' . $telefone_numeros;
                  }

                  $texto_whatsapp = "Venho por meio do Site Sistema de Eventos, dizer que estou interessado em fazer parte do seu evento $nomeUsuario";
                  $texto_whatsapp_url = urlencode($texto_whatsapp);
                  ?>

                  

                  <a class="btn btn-primary w-100 mb-2" href="https://wa.me/<?= $telefone_numeros ?>?text=<?= $texto_whatsapp_url ?>" role="button">
                      Entrar em Contato
                  </a>
                </div>
              </div>

            </aside>
          </div>
        </main>

        <?php else: ?>
        <p class="text-center mt-5">Evento não encontrado.</p>
        <?php endif; ?>

              

        

  <footer class="fixed-bottom bg-dark text-white text-center py-3">
    © Victor Emanuel da Silva Moraes
  </footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>